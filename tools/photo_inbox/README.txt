Drop song photos here.

Copy them straight off the phone (USB / cloud / whatever is easiest) rather
than sending them through the app. The app upload arrives re-encoded at 2048px
and roughly JPEG quality 35 with the EXIF stripped, which is what destroys the
ő and ű accents. The originals off the camera are full resolution.

Any name is fine. Say the word once they are here.

Scored with:  python tools/fixtures/score.py

---
phase: 01-bug-fixes-core-polish
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - songbook_app/lib/presentation/screens/song_view/widgets/chord_view.dart
  - songbook_app/lib/presentation/screens/song_view/song_view_screen.dart
  - songbook_app/lib/presentation/screens/song_view/widgets/floating_controls_menu.dart
autonomous: false

must_haves:
  truths:
    - "Chord view content (verses, metadata) is horizontally centered on screen, not left-aligned"
    - "User can increase text size while viewing a song via the floating controls menu"
    - "User can decrease text size while viewing a song via the floating controls menu"
    - "Text size changes are immediately visible in chord view"
    - "Text size controls appear in the floating controls menu alongside existing transpose controls"
  artifacts:
    - path: "songbook_app/lib/presentation/screens/song_view/widgets/chord_view.dart"
      provides: "Centered chord view layout"
      contains: "CrossAxisAlignment.center"
    - path: "songbook_app/lib/presentation/screens/song_view/widgets/floating_controls_menu.dart"
      provides: "Text size increase/decrease buttons in floating menu"
      contains: "increaseTextScale"
    - path: "songbook_app/lib/presentation/screens/song_view/song_view_screen.dart"
      provides: "textScale passed to ChordView"
      contains: "textScale"
  key_links:
    - from: "floating_controls_menu.dart"
      to: "song_provider.dart"
      via: "increaseTextScale/decreaseTextScale calls on songViewNotifier"
      pattern: "increaseTextScale|decreaseTextScale"
    - from: "song_view_screen.dart"
      to: "chord_view.dart"
      via: "textScale prop passed from songViewProvider to ChordView"
      pattern: "textScale.*textScale"
---

<objective>
Center the chord view layout to match the sheet music canvas centering, and ensure the in-song text size controls (A+/A- buttons already in floating menu) actually work by wiring them through the textScale state that already exists in SongViewState.

Purpose: Chord view content is currently left-aligned while sheet music is centered, creating visual inconsistency. Text size controls (A+/A-) already exist in the floating controls menu but need to be verified as working end-to-end. This plan ensures visual polish and functional text sizing.

Output: Centered chord view, working text size controls.
</objective>

<execution_context>
@C:\Users\rober\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\rober\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug-fixes-core-polish/01-01-SUMMARY.md
@songbook_app/lib/presentation/screens/song_view/widgets/chord_view.dart
@songbook_app/lib/presentation/screens/song_view/song_view_screen.dart
@songbook_app/lib/presentation/screens/song_view/widgets/floating_controls_menu.dart
@songbook_app/lib/presentation/providers/song_provider.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Center chord view layout and verify text size wiring</name>
  <files>
    songbook_app/lib/presentation/screens/song_view/widgets/chord_view.dart
    songbook_app/lib/presentation/screens/song_view/song_view_screen.dart
  </files>
  <action>
In `chord_view.dart`, center the chord view content:

1. In the `build()` method, change the outer Column's `crossAxisAlignment` from `CrossAxisAlignment.start` (line 39) to `CrossAxisAlignment.center`. This centers verses, metadata, and origin info horizontally.

2. Keep the inner verse Column (`_buildVerse`) at `CrossAxisAlignment.start` so that verse text itself remains left-aligned within its container. The verse as a block is centered on screen, but text within each verse stays left-justified (natural reading alignment).

3. To achieve "centered block of left-aligned text", wrap the inner Column content in an `IntrinsicWidth` widget or use a `ConstrainedBox` with max width. The recommended approach:
   - Wrap the `Column` inside `build()` with a `Center` widget, and set the Column's `crossAxisAlignment` back to `CrossAxisAlignment.start`
   - Add a `ConstrainedBox` with `maxWidth: 600` (or similar) around the Column to prevent it from stretching too wide on tablets/desktop while staying centered
   - This gives: centered container with max-width, left-aligned text inside

   Specifically, change the `SingleChildScrollView`'s child from:
   ```dart
   child: Column(
     crossAxisAlignment: CrossAxisAlignment.start,
     children: [ ... ],
   ),
   ```
   To:
   ```dart
   child: Center(
     child: ConstrainedBox(
       constraints: const BoxConstraints(maxWidth: 600),
       child: Column(
         crossAxisAlignment: CrossAxisAlignment.start,
         children: [ ... ],
       ),
     ),
   ),
   ```

4. In `song_view_screen.dart`, verify that `textScale` from `textScaleProvider` is already being passed to `ChordView`. Looking at the current code (line 94):
   ```dart
   ChordView(song: song, transpose: transpose, textScale: textScale)
   ```
   This is already correct. The textScale prop IS being passed. No change needed here.

5. Verify the ChordView constructor accepts `textScale` (it does, line 18, with default 1.0) and uses it (line 28: `final fontSize = baseFontSize * textScale`). This is already wired correctly.

6. The floating controls menu already has A+/A- buttons calling `songViewNotifier.increaseTextScale` and `songViewNotifier.decreaseTextScale` (lines 99 and 107). These methods exist in `SongViewNotifier` (song_provider.dart lines 91-103) and modify `textScale` in `SongViewState`. The `textScaleProvider` derives from `songViewProvider` state. This chain is already complete.

SUMMARY: The main change is centering the chord view layout. Text size controls are already wired end-to-end through the existing state management chain. The only code change is adding Center + ConstrainedBox to chord_view.dart.
  </action>
  <verify>
Run `flutter analyze` from `songbook_app/` directory — no new errors or warnings.
Read `chord_view.dart` and confirm:
- Center widget wraps the main Column
- ConstrainedBox with maxWidth is present
- Inner Column still uses CrossAxisAlignment.start
  </verify>
  <done>
Chord view content is horizontally centered on screen with a max-width constraint. Text within verses remains left-aligned for readability. On wide screens (tablets, desktop), content doesn't stretch edge-to-edge.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    1. Chord view layout is now centered (matching sheet music canvas centering)
    2. Text size A+/A- controls in floating menu adjust chord view font size
    3. (From Plan 01-01) Transpose wrapping is symmetric -6 to +5
    4. (From Plan 01-01) Transpose state resets between songs
    5. (From Plan 01-01) SVG fallback messages are distinct
  </what-built>
  <how-to-verify>
    1. Run the app: `cd songbook_app && flutter run`
    2. Open any song with chords (e.g., song with structured verses)
    3. VERIFY CENTERING: Chord view content should be centered on screen, not left-aligned. On a wide screen, content should have margins on both sides.
    4. VERIFY TEXT SIZE: Tap the floating controls toggle (tune icon on right edge). Press A+ button 3 times — text should visibly grow. Press A- button 5 times — text should visibly shrink below original size.
    5. VERIFY TRANSPOSE WRAPPING: Press T+ (transpose up) repeatedly. After reaching +5, the next press should show -6. Press T- from -6 and it should show +5.
    6. VERIFY STATE RESET: Transpose a song to +3, then navigate back to song list and open a different song. The new song should show transpose = 0.
    7. VERIFY SVG FALLBACK: Switch to sheet music view for a song that has notation. If a transposed SVG is missing, message should indicate "showing original key" not just "not available".
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Run `flutter analyze` from `songbook_app/` — zero errors, zero new warnings
2. Visual check: chord view centered on screen
3. Functional check: A+/A- buttons change text size in real-time
4. Integration check: all Phase 1 success criteria met (from Plan 01-01 + 01-02)
</verification>

<success_criteria>
- Chord view content is horizontally centered, not left-aligned
- Content has max-width constraint (doesn't stretch on tablets)
- Text inside verses remains left-aligned (readable)
- A+ button increases text size, A- decreases it, changes are immediate
- All Phase 1 success criteria from ROADMAP.md are met
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug-fixes-core-polish/01-02-SUMMARY.md`
</output>

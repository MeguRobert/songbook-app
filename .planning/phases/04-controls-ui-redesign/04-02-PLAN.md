---
phase: 04-controls-ui-redesign
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - songbook_app/lib/presentation/screens/song_view/song_view_screen.dart
  - songbook_app/lib/presentation/screens/song_view/widgets/floating_controls_menu.dart
autonomous: false

must_haves:
  truths:
    - "Presentation mode button visible in app bar alongside favorite button"
    - "Tapping presentation button opens full-screen presentation mode"
    - "Old floating controls column is completely removed from codebase"
    - "App builds and runs without any reference to deleted floating_controls_menu.dart"
  artifacts:
    - path: "songbook_app/lib/presentation/screens/song_view/song_view_screen.dart"
      provides: "App bar with presentation + favorite buttons, FAB for controls, pinch-to-zoom"
      contains: "Icons.fullscreen"
  key_links:
    - from: "song_view_screen.dart app bar"
      to: "presentation route"
      via: "context.push(AppRoutes.presentationPath(songNumber))"
      pattern: "presentationPath"
---

<objective>
Move the presentation mode button to the app bar for quick access, verify the Custom view option works correctly in the bottom sheet, and remove the old floating controls menu file -- completing the Phase 4 UI redesign.

Purpose: Finish the controls redesign by surfacing presentation mode in the app bar (not buried in controls) and cleaning up the replaced code.
Output: Updated `song_view_screen.dart` with app bar presentation button, deleted `floating_controls_menu.dart`.
</objective>

<execution_context>
@C:\Users\rober\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\rober\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-controls-ui-redesign/04-01-SUMMARY.md

@songbook_app/lib/presentation/screens/song_view/song_view_screen.dart
@songbook_app/lib/presentation/screens/song_view/widgets/floating_controls_menu.dart
@songbook_app/lib/router/app_router.dart
@songbook_app/lib/presentation/screens/song_view/widgets/song_controls_sheet.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add presentation mode button to app bar and verify Custom view option</name>
  <files>songbook_app/lib/presentation/screens/song_view/song_view_screen.dart</files>
  <action>
**1. Add presentation mode to app bar:**

In the AppBar `actions` list, add a presentation mode IconButton BEFORE the existing favorite button:

```dart
actions: [
  // Presentation mode button
  IconButton(
    icon: const Icon(Icons.fullscreen),
    onPressed: () {
      context.push(AppRoutes.presentationPath(widget.songNumber));
    },
    tooltip: 'Presentation mode',
  ),
  // Favorite button (existing)
  IconButton(
    icon: Icon(
      isFavorite ? Icons.favorite : Icons.favorite_border,
      color: isFavorite ? Colors.red : null,
    ),
    onPressed: () { ... },
    tooltip: isFavorite ? 'Remove from favorites' : 'Add to favorites',
  ),
],
```

Ensure `go_router` and `app_router.dart` are imported (go_router for `context.push`, app_router for `AppRoutes`). Check if they are already imported from Plan 01 changes; add if missing.

**2. Verify Custom view option in bottom sheet:**

Open `song_controls_sheet.dart` and verify:
- When the current ViewConfig matches none of the 3 presets (isNotationWithoutChords == true), the "Custom" chip should be visually selected.
- When "Custom" is tapped, the notation and chords SwitchListTiles should be visible and functional.
- When a preset chip is tapped after being in Custom mode, the SwitchListTiles should collapse.
- If there are any issues, fix them. This is a verification + fix task, not a full rewrite.

**3. Clean up any remaining references:**

Ensure `song_view_screen.dart` does NOT import `floating_controls_menu.dart`. If Plan 01 already removed it, confirm. If there's a lingering import, remove it.
  </action>
  <verify>
Run `flutter analyze` from the songbook_app directory -- no errors.
Verify with grep: `Icons.fullscreen` appears in song_view_screen.dart.
Verify with grep: `presentationPath` appears in song_view_screen.dart.
Verify with grep: `floating_controls_menu` does NOT appear in song_view_screen.dart.
  </verify>
  <done>
App bar shows presentation mode (fullscreen icon) and favorite buttons. Presentation button navigates to /presentation/:id route. Custom view option in bottom sheet correctly reveals notation/chords toggles when selected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete old floating controls menu and verify clean build</name>
  <files>songbook_app/lib/presentation/screens/song_view/widgets/floating_controls_menu.dart</files>
  <action>
**1. Search for any remaining references to floating_controls_menu.dart:**

Run a grep/search across the entire songbook_app/lib directory for:
- `floating_controls_menu` (import references)
- `FloatingControlsMenu` (class usage)

If any references remain beyond the file itself, remove them. Expected: zero references after Plan 01 replaced the widget in song_view_screen.dart.

**2. Delete the file:**

Delete `songbook_app/lib/presentation/screens/song_view/widgets/floating_controls_menu.dart` (the 513-line old floating column widget).

**3. Verify clean build:**

Run `flutter analyze` from the songbook_app directory. Must pass with zero errors.

If there are any errors referencing the deleted file or missing imports, fix them.
  </action>
  <verify>
1. `flutter analyze` passes with zero errors.
2. The file `songbook_app/lib/presentation/screens/song_view/widgets/floating_controls_menu.dart` does not exist.
3. No Dart file in the project contains the string `floating_controls_menu`.
4. No Dart file in the project contains `FloatingControlsMenu` (the class name).
  </verify>
  <done>
Old floating_controls_menu.dart is deleted. No references to it remain in the codebase. `flutter analyze` passes cleanly. The entire Phase 4 controls redesign is complete.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Controls UI Redesign (Phase 4):
- FAB (tune icon) on song view opens a bottom sheet with labeled View, Transpose, and Text Size sections
- View section has 3 preset chips (Sheet Music, Chords, Lyrics) plus a Custom option with individual toggles
- Transpose section has +/- buttons with key display and conditional reset button
- Text Size section has A-/A+ buttons with scale percentage display
- Pinch-to-zoom gesture on song content scales text
- Presentation mode button in app bar (fullscreen icon, before favorite heart)
- Old 513-line floating controls column completely removed
  </what-built>
  <how-to-verify>
1. Run the app: `flutter run` from songbook_app/
2. Navigate to any song
3. Verify the app bar shows: back arrow, song title, fullscreen icon, favorite heart
4. Tap the fullscreen icon -- should open presentation mode
5. Go back to song view
6. Verify a small FAB (tune icon) is visible at bottom-right
7. Tap the FAB -- bottom sheet should open with 3 labeled sections
8. In View section: tap each preset chip, verify song view updates behind the sheet
9. Tap "Custom" chip -- verify notation and chords switches appear
10. Toggle notation switch -- verify song view changes
11. Toggle chords switch -- verify song view changes
12. In Transpose section: tap + and - buttons, verify key display updates
13. If transposed, verify "Reset to [key]" button appears and works
14. In Text Size section: tap A+ and A- buttons, verify scale percentage updates and text size changes
15. Close the sheet, pinch-to-zoom on the song content -- verify text scales
16. Verify the old floating column of buttons on the right side is completely gone
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `flutter analyze` passes with zero errors
2. App bar has 2 action buttons: presentation mode (fullscreen) and favorite (heart)
3. FAB (tune icon) opens bottom sheet with View, Transpose, Text Size sections
4. Pinch-to-zoom works on song content
5. No file named `floating_controls_menu.dart` exists in the project
6. No Dart file references `FloatingControlsMenu` or `floating_controls_menu`
7. All 5 Phase 4 success criteria are met:
   a. Floating column replaced by FAB + bottom sheet
   b. Pinch-to-zoom works for text scaling
   c. Three presets primary, Custom option available
   d. Presentation mode in app bar
   e. Transpose controls clearly labeled in bottom sheet
</verification>

<success_criteria>
- Presentation mode button visible in app bar, functional
- Old floating_controls_menu.dart deleted, zero references remain
- Custom view option properly shows/hides individual toggles
- flutter analyze passes clean
- All Phase 4 success criteria verified via human UAT
</success_criteria>

<output>
After completion, create `.planning/phases/04-controls-ui-redesign/04-02-SUMMARY.md`
</output>

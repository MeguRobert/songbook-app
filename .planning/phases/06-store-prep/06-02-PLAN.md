---
phase: 06-store-prep
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - songbook_app/android/app/src/main/AndroidManifest.xml
  - songbook_app/android/app/build.gradle.kts
  - songbook_app/ios/Runner/Info.plist
  - songbook_app/flutter_launcher_icons.yaml
  - songbook_app/flutter_native_splash.yaml
  - songbook_app/assets/icons/README.md
  - .planning/phases/06-store-prep/STORE-LISTING.md
  - .planning/phases/06-store-prep/RELEASE-CHECKLIST.md
autonomous: true

must_haves:
  truths:
    - "App display name is the user-facing 'Songbook' on Android (android:label) and iOS (CFBundleDisplayName)"
    - "Android declares no runtime permissions (offline/local-first) and this is documented in the manifest"
    - "iOS Info.plist declares no privacy usage strings (none needed) — documented"
    - "Bundle id (com.songbook.songbook_app) and version (1.0.0+1) are confirmed/documented; stale Android applicationId TODO removed"
    - "flutter_launcher_icons + flutter_native_splash config files exist, point at a placeholder source, and are flagged 'needs final art'"
    - "Store-listing metadata (title, short/full description, keywords, category, content rating, screenshot list) is drafted for Robert"
    - "A release checklist captures the remaining human/online steps (art, dep install, regen, signing, screenshots, submission)"
  artifacts:
    - path: "songbook_app/flutter_launcher_icons.yaml"
      provides: "Launcher-icon generator config (placeholder source, all platforms)"
      min_lines: 15
    - path: "songbook_app/flutter_native_splash.yaml"
      provides: "Native splash generator config (placeholder, light+dark)"
      min_lines: 12
    - path: ".planning/phases/06-store-prep/STORE-LISTING.md"
      provides: "Drafted Play/App Store listing copy + metadata"
      min_lines: 60
    - path: ".planning/phases/06-store-prep/RELEASE-CHECKLIST.md"
      provides: "Human/online step checklist to ship"
      min_lines: 30
  key_links:
    - from: "AndroidManifest.xml"
      to: "app display name"
      via: "android:label"
      pattern: "android:label=\"Songbook\""
---

<objective>
Deliver the codeable + drafted slice of Phase 6 success criteria #1, #2, #5: wire the app's
user-facing name, confirm a minimal permission surface, set up launcher-icon/splash generator config
(placeholder-flagged, since no on-brand source art exists), and draft the store-listing metadata and a
release checklist. The actual icon/splash regeneration, real artwork, release signing, screenshots,
and store submission are human/online steps recorded as blockers — not faked.

Output: platform manifest/config edits, two generator config files, an assets/icons README,
STORE-LISTING.md, RELEASE-CHECKLIST.md.
</objective>

<context>
@songbook_app/pubspec.yaml
@songbook_app/android/app/src/main/AndroidManifest.xml
@songbook_app/android/app/build.gradle.kts
@songbook_app/ios/Runner/Info.plist
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set user-facing app display name</name>
  <files>AndroidManifest.xml, Info.plist</files>
  <action>
Android: change `android:label="songbook_app"` → `android:label="Songbook"`.
iOS: set `CFBundleDisplayName` → `Songbook` (was "Songbook App"); leave `CFBundleName`
(`$(EXECUTABLE_NAME)`-adjacent internal name) as-is or set to "Songbook" (≤15 chars OK).
Do not touch `CFBundleIdentifier` (driven by Xcode build settings / PRODUCT_BUNDLE_IDENTIFIER).
  </action>
  <verify>android:label="Songbook" present; CFBundleDisplayName is Songbook.</verify>
  <done>Home-screen/app name reads "Songbook" on both platforms.</done>
</task>

<task type="auto">
  <name>Task 2: Document the minimal permission surface</name>
  <files>AndroidManifest.xml, Info.plist, build.gradle.kts</files>
  <action>
Android: the app is offline/local-first and declares NO `<uses-permission>` (not even INTERNET).
Add an XML comment above `<application>` documenting this intentional choice so a future edit doesn't
silently add permissions. Keep the existing PROCESS_TEXT `<queries>` (Flutter engine requirement).
iOS: add a comment in Info.plist noting no privacy usage-description keys are required (no camera,
mic, location, photo, contacts, tracking). build.gradle.kts: remove the now-inaccurate
"TODO: Specify your own unique Application ID" comment (the id IS set); leave the signing TODO (real
human step) but tighten its wording to point at RELEASE-CHECKLIST.md.
  </action>
  <verify>flutter analyze still clean; manifest has the no-permissions comment; no uses-permission added.</verify>
  <done>Minimal-permission posture is explicit and documented on both platforms.</done>
</task>

<task type="auto">
  <name>Task 3: Launcher-icon + splash generator config (placeholder-flagged)</name>
  <files>flutter_launcher_icons.yaml, flutter_native_splash.yaml, assets/icons/README.md</files>
  <action>
assets/icons/ currently holds only .gitkeep — there is NO source art, so we CANNOT regenerate real
branding and must not fabricate it. Create the generator configs pointing at a placeholder source
`assets/icons/app_icon.png` (and `app_icon_foreground.png` for adaptive), with a prominent
"NEEDS FINAL ART" banner comment in each file:
- flutter_launcher_icons.yaml: android + ios true, adaptive icon background/foreground, min_sdk_android.
- flutter_native_splash.yaml: color (light) + color_dark, image placeholder, android12 block.
Create assets/icons/README.md explaining: required source files + sizes, the `flutter pub add` line
for the two dev deps (NOT added here because the sandbox has no network and they are not cached), and
the regen commands. Do NOT modify pubspec.yaml dependencies (keeps `flutter pub get`/analyze/test
green offline).
  </action>
  <verify>Both yaml files exist with placeholder paths + NEEDS FINAL ART banner; assets/icons/README.md documents deps + regen commands; flutter analyze/test unaffected.</verify>
  <done>Icon/splash generation is fully wired and one art-drop + two commands away from done.</done>
</task>

<task type="auto">
  <name>Task 4: Draft store-listing metadata</name>
  <files>.planning/phases/06-store-prep/STORE-LISTING.md</files>
  <action>
Draft (for Robert to edit) Google Play + Apple App Store copy: app name, Play short description
(≤80 chars), full description (≤4000 chars, feature bullets), Apple subtitle (≤30) + promotional text
(≤170) + keywords (≤100 chars comma list), primary/secondary category, content rating notes
(no objectionable content; Everyone / 4+), data-safety / privacy note (no data collected, no network,
local-only), support contact placeholder, and a screenshot shot-list (which screens, phone+tablet).
Mark every literal store value Robert must confirm with «...».
  </action>
  <verify>STORE-LISTING.md covers both stores with length-limited fields and a screenshot list.</verify>
  <done>Listing copy is ready for Robert to review/submit.</done>
</task>

<task type="auto">
  <name>Task 5: Release checklist (human/online steps)</name>
  <files>.planning/phases/06-store-prep/RELEASE-CHECKLIST.md</files>
  <action>
Capture the remaining non-codeable steps in order: supply final art → `flutter pub add` the two gen
deps (needs network) → run icon/splash regen → create release signing keystore + configure
`build.gradle.kts` signingConfig (Android) and Apple signing/provisioning → build appbundle/IPA →
capture screenshots on device/emulator → fill STORE-LISTING.md literals → set up Play Console / App
Store Connect → submit. Note on-device crash/perf testing (criterion #3) and a11y screen-reader UAT
(from 06-01) as required human verification.
  </action>
  <verify>RELEASE-CHECKLIST.md lists ordered human/online steps incl. signing, regen, screenshots, submission.</verify>
  <done>The path from this branch to a submitted build is explicit.</done>
</task>

</tasks>

<verification>
1. `flutter analyze` — no new issues beyond the 8 pre-existing settings_screen.dart deprecation infos.
2. `flutter test` — 23/23 still pass (no app-code behavior change).
3. Display name = "Songbook"; no new permissions; generator configs + docs present.
</verification>

<success_criteria>
- App icon & splash configured (criterion #1) — config wired; final art flagged as blocker.
- Store metadata drafted (criterion #2) — copy ready; screenshots flagged as blocker.
- Proper permissions / no placeholder content posture (criterion #5) — minimal permissions documented.
- (criterion #3 on-device stability) — recorded as human verification in the checklist.
</success_criteria>

<output>
After completion, create `.planning/phases/06-store-prep/06-02-SUMMARY.md`.
</output>

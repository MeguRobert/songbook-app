---
phase: 06-store-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - songbook_app/lib/presentation/screens/song_list/widgets/song_list_tile.dart
  - songbook_app/lib/presentation/screens/settings/settings_screen.dart
  - songbook_app/lib/presentation/screens/song_view/widgets/song_controls_sheet.dart
  - songbook_app/lib/presentation/widgets/sheet_music/sheet_music_renderer.dart
  - songbook_app/lib/presentation/screens/presentation/presentation_screen.dart
  - songbook_app/test/accessibility/accessibility_test.dart
autonomous: true

must_haves:
  truths:
    - "Icon-only interactive controls have screen-reader labels (tooltip or Semantics)"
    - "Section headers are exposed to screen readers as headers (Semantics(header: true))"
    - "Song list tiles read as a meaningful unit ('Song N: title') rather than a bare number"
    - "The sheet-music canvas (CustomPaint, invisible to a11y tree) carries a descriptive Semantics label"
    - "App content respects system text scaling (no MediaQuery textScaler override that disables it)"
    - "Automated accessibility-guideline tests (labeled tap targets, tap target size) pass on the song list + settings"
    - "A text-scaling smoke test pumps a screen at a large scale without overflow/exception"
  artifacts:
    - path: "songbook_app/test/accessibility/accessibility_test.dart"
      provides: "Guideline + semantics + text-scaling widget tests"
      min_lines: 90
  key_links:
    - from: "settings_screen.dart"
      to: "font-size IconButtons"
      via: "tooltip added so labeledTapTargetGuideline passes"
      pattern: "tooltip:"
---

<objective>
Deliver the codeable, testable slice of Phase 6 success criterion #4 ("Basic accessibility — text
scaling, screen reader labels — works"). Add Semantics labels / screen-reader text to the major
screens, ensure icon-only controls are labeled, mark section headers, give the invisible sheet-music
canvas a descriptive label, and confirm the app honours system text scaling. Back it with automated
accessibility-guideline tests and a text-scaling smoke test.

Purpose: real, on-device-verifiable accessibility that also passes `flutter test` headless.
Output: Semantics improvements across song list / settings / controls sheet / sheet-music renderer /
presentation screen, plus `test/accessibility/accessibility_test.dart`.
</objective>

<context>
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@songbook_app/test/presentation/providers/book_provider_test.dart  (provider-override test pattern)
@songbook_app/lib/presentation/screens/song_list/widgets/song_list_tile.dart
@songbook_app/lib/presentation/screens/settings/settings_screen.dart
@songbook_app/lib/presentation/screens/song_view/widgets/song_controls_sheet.dart
@songbook_app/lib/presentation/widgets/sheet_music/sheet_music_renderer.dart
@songbook_app/lib/presentation/screens/presentation/presentation_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Label icon-only controls + mark section headers</name>
  <files>settings_screen.dart, song_controls_sheet.dart</files>
  <action>
settings_screen.dart:
- The Font Size −/+ `IconButton`s (Icons.remove / Icons.add) have NO tooltip — add
  `tooltip: 'Decrease font size'` and `tooltip: 'Increase font size'`. (This is what
  labeledTapTargetGuideline flags.)
- Wrap the `_buildSectionHeader` Text in `Semantics(header: true, child: ...)`.

song_controls_sheet.dart:
- The A−/A+ `TextButton`s read as cryptic "A-"/"A+" to a screen reader — wrap each child Text in
  `Semantics(label: 'Decrease text size' / 'Increase text size', child: ...)` (keep the visible
  'A-'/'A+' text via excludeSemantics on the inner Text, or pass label on a Semantics wrapper around
  the Text and set excludeSemantics: true).
- Wrap `_SectionHeader`'s Text in `Semantics(header: true, child: ...)`.
  </action>
  <verify>flutter analyze clean. tooltip: appears twice in settings_screen.dart for the font buttons.</verify>
  <done>Every icon/letter-only control has a meaningful SR label; section headers announce as headers.</done>
</task>

<task type="auto">
  <name>Task 2: Make song list tiles read as a unit</name>
  <files>song_list_tile.dart</files>
  <action>
Wrap the leading number `Text` in `Semantics(label: 'Song ${song.number}', excludeSemantics: true,
child: Text(...))` so a screen reader announces "Song 42" instead of a bare "42". Leave the title,
reference (subtitle) and the favorite IconButton (already tooltip-labeled) as-is so they remain
distinct, navigable nodes.
  </action>
  <verify>flutter analyze clean.</verify>
  <done>List tiles expose 'Song N' for the leading number; favorite action stays independently labeled.</done>
</task>

<task type="auto">
  <name>Task 3: Describe the sheet-music canvas to screen readers</name>
  <files>sheet_music_renderer.dart</files>
  <action>
The `CustomPaint` inside the `RepaintBoundary` paints notation on a canvas that is invisible to the
accessibility tree. Wrap that `RepaintBoundary` in
`Semantics(label: 'Sheet music notation for ${widget.song.title}', image: true, child: ...)` so SR
users get a description instead of silence.
  </action>
  <verify>flutter analyze clean.</verify>
  <done>The notation canvas has an image Semantics label.</done>
</task>

<task type="auto">
  <name>Task 4: Presentation-mode header semantics (light touch)</name>
  <files>presentation_screen.dart</files>
  <action>
Mark the title card as a header for SR: wrap the title `Text` in `_buildTitleCard` with
`Semantics(header: true, child: ...)`. (Verse text is already exposed via Text widgets; tap zones are
a projection convenience — do not over-engineer.) Keep changes minimal and behavior-preserving.
  </action>
  <verify>flutter analyze clean.</verify>
  <done>Presentation title announces as a header; no behavioral change.</done>
</task>

<task type="auto">
  <name>Task 5: Accessibility + text-scaling tests</name>
  <files>test/accessibility/accessibility_test.dart</files>
  <action>
Create widget tests (pattern from book_provider_test.dart for ProviderScope overrides:
sharedPreferencesProvider + songsProvider). Use a fixture song WITH a reference and at least one
verse so the list/tiles render. Wrap the screen under test in `ProviderScope(overrides: ...)` +
`MaterialApp(home: ...)`.

Tests:
1. SongListScreen meets `androidTapTargetGuideline` and `labeledTapTargetGuideline`:
   ```
   final handle = tester.ensureSemantics();
   await tester.pumpWidget(...); await tester.pumpAndSettle();
   await expectLater(tester, meetsGuideline(androidTapTargetGuideline));
   await expectLater(tester, meetsGuideline(labeledTapTargetGuideline));
   handle.dispose();
   ```
2. SettingsScreen meets `labeledTapTargetGuideline` (proves the font-size buttons are now labeled).
3. Semantics: SongListScreen exposes a 'Song 1' label (find.bySemanticsLabel(RegExp('Song 1'))).
4. Text-scaling smoke: pump SongListScreen inside
   `MediaQuery(data: MediaQueryData(textScaler: TextScaler.linear(2.5)), child: ...)`,
   pumpAndSettle, then `expect(tester.takeException(), isNull)`.

Run the file; if a guideline genuinely fails, fix the underlying UI (that is the point of the phase)
or, if the failure is a framework artifact unrelated to our screens, narrow the assertion and note it
in the SUMMARY. Do not commit a failing/over-broad test.
  </action>
  <verify>flutter test test/accessibility/accessibility_test.dart passes.</verify>
  <done>Accessibility guidelines + semantics labels + text scaling are covered by passing tests.</done>
</task>

</tasks>

<verification>
1. `flutter analyze` — no new issues beyond the 8 pre-existing settings_screen.dart deprecation infos.
2. `flutter test` — all tests pass, including the new accessibility_test.dart.
</verification>

<success_criteria>
- Screen-reader labels present on major screens (criterion #4, "screen reader labels").
- Text scaling works / is honoured (criterion #4, "text scaling").
</success_criteria>

<output>
After completion, create `.planning/phases/06-store-prep/06-01-SUMMARY.md`.
</output>

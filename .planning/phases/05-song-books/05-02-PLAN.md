---
phase: 05-song-books
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - songbook_app/lib/presentation/providers/book_provider.dart
  - songbook_app/lib/presentation/screens/books/book_browser_screen.dart
  - songbook_app/lib/presentation/screens/song_list/song_list_screen.dart
  - songbook_app/lib/router/app_router.dart
  - songbook_app/test/presentation/providers/book_provider_test.dart
autonomous: true

must_haves:
  truths:
    - "A book browser screen lists All Songs + every book with its song count"
    - "Selecting a book in the browser filters the song list to that book"
    - "The 'All Songs' entry restores the unfiltered list across all books"
    - "The song list app bar shows the active book name and an entry point to the book browser"
    - "Selected book persists across app restart (via SettingsRepository)"
    - "Currently-selected book is visually indicated in the browser (check mark)"
  artifacts:
    - path: "songbook_app/lib/presentation/providers/book_provider.dart"
      provides: "booksProvider, selectedBookProvider (persisted), filteredSongsProvider"
      min_lines: 50
    - path: "songbook_app/lib/presentation/screens/books/book_browser_screen.dart"
      provides: "Book browser UI with All Songs + per-book tiles and selection state"
      min_lines: 70
  key_links:
    - from: "song_list_screen.dart"
      to: "book_provider.dart"
      via: "watches filteredSongsProvider + selectedBookProvider"
      pattern: "filteredSongsProvider"
    - from: "book_browser_screen.dart"
      to: "book_provider.dart"
      via: "ref.read(selectedBookProvider.notifier).select(...)/clear()"
      pattern: "selectedBookProvider.notifier"
    - from: "app_router.dart"
      to: "book_browser_screen.dart"
      via: "GoRoute '/books'"
      pattern: "/books"
---

<objective>
Build the book browser UI and wire book filtering into the song list. Selecting a book filters the
home list; "All Songs" clears the filter; the active book is reflected in the app bar; the choice
persists across restarts.

Purpose: Phase 5 success criteria #2 (book browser to select a book), #3 (song list filters by
selected book), and #4 (All Songs view still available).
Output: New book_provider.dart, BookBrowserScreen + route, updated SongListScreen, provider tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

@songbook_app/lib/presentation/providers/providers.dart
@songbook_app/lib/presentation/providers/song_provider.dart
@songbook_app/lib/presentation/providers/favorites_provider.dart
@songbook_app/lib/presentation/screens/song_list/song_list_screen.dart
@songbook_app/lib/presentation/screens/song_list/widgets/song_list_tile.dart
@songbook_app/lib/router/app_router.dart
@songbook_app/lib/data/models/book.dart
@songbook_app/lib/domain/services/book_service.dart
@songbook_app/lib/data/repositories/settings_repository.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Book providers (books list, selected book, filtered songs)</name>
  <files>songbook_app/lib/presentation/providers/book_provider.dart</files>
  <action>
Create a new provider file (do NOT modify the WIP-edited settings_provider.dart / song_provider.dart):

- `bookServiceProvider` = `Provider<BookService>((ref) => const BookService());`
- `booksProvider` = `FutureProvider<List<Book>>` → `final songs = await ref.watch(songsProvider.future);
  return ref.watch(bookServiceProvider).booksFromSongs(songs);`
- `SelectedBookNotifier extends StateNotifier<String?>`:
  - constructor reads `settingsRepositoryProvider.getSelectedBook()` for initial state (persisted).
  - `Future<void> select(String bookName)` → persist via `setSelectedBook`, set state.
  - `Future<void> clear()` → `clearSelectedBook()`, set state = null (All Songs).
- `selectedBookProvider` = `StateNotifierProvider<SelectedBookNotifier, String?>`.
- `filteredSongsProvider` = `FutureProvider<List<Song>>`:
  - `final songs = await ref.watch(songsProvider.future);`
  - `final selected = ref.watch(selectedBookProvider);`
  - `return ref.watch(bookServiceProvider).filterByBook(songs, selected);`

Import models/services/providers as needed. Document each provider with `///`.
  </action>
  <verify>`flutter analyze` clean for book_provider.dart.</verify>
  <done>Providers expose the book list, the persisted selected book, and the filtered song list.</done>
</task>

<task type="auto">
  <name>Task 2: BookBrowserScreen</name>
  <files>songbook_app/lib/presentation/screens/books/book_browser_screen.dart</files>
  <action>
Create `BookBrowserScreen` (ConsumerWidget) at route `/books`:
- AppBar title "Books".
- Body watches `booksProvider` (AsyncValue.when with loading/error like SongListScreen) and
  `selectedBookProvider`.
- A ListView with:
  - A leading "All Songs" ListTile (leading Icon(Icons.library_music)), trailing a check icon when
    `selected == null`. onTap → `ref.read(selectedBookProvider.notifier).clear()` then
    `context.pop()`. Show total song count as subtitle (sum of book counts or songsProvider length).
  - A Divider.
  - One ListTile per Book: leading Icon(Icons.menu_book), title = book.name, subtitle =
    '${book.songCount} songs', trailing check icon when `selected == book.name`. onTap →
    `selectedBookProvider.notifier.select(book.name)` then `context.pop()`.
- Empty/error states consistent with SongListScreen styling.
Use `context.pop()` (GoRouter) to return to the list after selection.
  </action>
  <verify>`flutter analyze` clean. Screen compiles and references selectedBookProvider + booksProvider.</verify>
  <done>Browser lists All Songs + each book with counts and a check mark on the active selection.</done>
</task>

<task type="auto">
  <name>Task 3: Route the book browser</name>
  <files>songbook_app/lib/router/app_router.dart</files>
  <action>
Add `static const books = '/books';` to AppRoutes. Add a GoRoute (outside the shell, like search):
`GoRoute(path: AppRoutes.books, name: 'books', builder: (context, state) => const BookBrowserScreen())`.
Import book_browser_screen.dart.
  </action>
  <verify>`flutter analyze` clean. '/books' route present.</verify>
  <done>Book browser reachable via push to /books.</done>
</task>

<task type="auto">
  <name>Task 4: Integrate filtering into SongListScreen</name>
  <files>songbook_app/lib/presentation/screens/song_list/song_list_screen.dart</files>
  <action>
- Watch `filteredSongsProvider` instead of `songsProvider` for the list body.
- Watch `selectedBookProvider`; set the AppBar title to the selected book name, or 'Songbook' when
  null. (Optionally append a small subtitle/label; keep it simple.)
- Add an AppBar action IconButton (before search) with Icons.menu_book (use Icons.menu_book when a
  book is selected and Icons.menu_book_outlined when null, to signal active filter), tooltip 'Books',
  onPressed → `context.push(AppRoutes.books)`.
- Update the empty state: when a book is selected but has no songs, show a message naming the book
  and a 'Show all songs' TextButton calling `ref.read(selectedBookProvider.notifier).clear()`.
- Keep the existing favorite toggle / navigation behavior unchanged.
Note: filteredSongsProvider already depends on songsProvider, so loading/error propagate.
  </action>
  <verify>`flutter analyze` clean. song_list_screen.dart references filteredSongsProvider and selectedBookProvider and AppRoutes.books.</verify>
  <done>Home list filters by selected book; app bar reflects the active book; All Songs restores full list.</done>
</task>

<task type="auto">
  <name>Task 5: Provider tests for selection + filtering</name>
  <files>songbook_app/test/presentation/providers/book_provider_test.dart</files>
  <action>
Using ProviderContainer + `SharedPreferences.setMockInitialValues({})` (per TESTING.md):
- Override `songsProvider` with a fixed list spanning two books + one null-book song.
- Override `sharedPreferencesProvider` with `await SharedPreferences.getInstance()` after mock init.
- Test booksProvider returns expected ordered books with counts (incl. 'Other').
- Test filteredSongsProvider returns all songs when selected is null.
- Test selecting a book (`selectedBookProvider.notifier.select('X')`) filters filteredSongsProvider
  to that book, and that the value is written to prefs (selected_book key present).
- Test clear() restores All Songs and removes the pref.
- Test initial selectedBookProvider state reads a pre-seeded `settings_selected_book` pref value.
Read providers via `await container.read(filteredSongsProvider.future)` and
`container.read(selectedBookProvider)`.
  </action>
  <verify>`flutter test test/presentation/providers/book_provider_test.dart` passes.</verify>
  <done>Selection, persistence, and filtering are covered by passing tests.</done>
</task>

</tasks>

<verification>
1. `flutter analyze` — no new issues beyond the 8 pre-existing infos.
2. `flutter test` — all tests pass (book_service + book_provider + existing widget_test).
3. Manual review: /books route renders, selection updates list, app bar reflects book, persists.
   (Visual UAT deferred to Robert's morning review — no device/browser available overnight.)
</verification>

<success_criteria>
- Book browser to select a book — Phase 5 criterion #2.
- Song list filters by selected book — Phase 5 criterion #3.
- All Songs view still available — Phase 5 criterion #4.
</success_criteria>

<output>
After completion, create `.planning/phases/05-song-books/05-02-SUMMARY.md` and `05-VERIFICATION.md`.
</output>
